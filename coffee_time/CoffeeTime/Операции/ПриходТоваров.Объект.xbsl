@Обработчик
метод ПередЗаписью(До: ПриходТоваров.Данные, ПараметрыЗаписи: ПриходТоваров.ПараметрыЗаписи)
    
    если не Проведен
        Проведен = (ПараметрыЗаписи.РежимЗаписиДокумента == РежимЗаписиДокумента.Проведение) 
    иначе
        Проведен = не (ПараметрыЗаписи.РежимЗаписиДокумента == РежимЗаписиДокумента.ОтменаПроведения)   
    ;
    
    СуммаДокумента = 0
    
    для СтрокаТЧ из Товары
        СуммаДокумента += СтрокаТЧ.Сумма
    ;
    
;